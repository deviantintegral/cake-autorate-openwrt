# STUB -- owned by task 6 (collectd statistics source).
#
# Task 2 created this file so the package reserves
# /etc/collectd/conf.d/cake-autorate.conf. Task 6 replaces the body with the
# real collectd `tail` plugin stanza.
#
# Facts task 6 needs (docs/upstream-option-inventory.md section 3):
#   * log path: /var/log/cake-autorate.<instance_id>.log
#     (default instance id is "primary")
#   * rotation leaves /var/log/cake-autorate.<instance_id>.log.old behind
#   * SUMMARY lines: 13 "; "-separated fields, field 0 == "SUMMARY"
#       3=dl_achieved_kbps 4=ul_achieved_kbps 5=dl_sum_delays 6=ul_sum_delays
#       7=dl_avg_owd_delta_us 8=ul_avg_owd_delta_us
#       9=dl_load_condition 10=ul_load_condition
#       11=cake_dl_rate_kbps 12=cake_ul_rate_kbps
#   * the SUMMARY_HEADER line has no TYPE/datetime/timestamp prefix
#
# Intentionally inert until task 6 lands: an empty file is valid collectd
# config, so shipping this stub cannot break a running collectd.
